# Skill: java-concurrency

## When to load
- When you need guidance related to **java-concurrency**.

## Load-on-demand flow
1) Read `docs/java-concurrency/_index.md` to pick the right chunk(s).
2) Load only the chunk(s) needed for the current task (avoid loading everything).

## Quick start
- Start with `docs/java-concurrency/chunk-01.md`.
