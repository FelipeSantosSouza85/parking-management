# Skill: java-object-oriented-design

## When to load
- When you need guidance related to **java-object-oriented-design**.

## Load-on-demand flow
1) Read `docs/java-ood/_index.md` to pick the right chunk(s).
2) Load only the chunk(s) needed for the current task (avoid loading everything).

## Quick start
- Start with `docs/java-ood/chunk-01.md`.
