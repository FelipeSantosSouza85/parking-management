# Skill: java-logging

## When to load
- When you need guidance related to **java-logging**.

## Load-on-demand flow
1) Read `docs/java-logging/_index.md` to pick the right chunk(s).
2) Load only the chunk(s) needed for the current task (avoid loading everything).

## Quick start
- Start with `docs/java-logging/chunk-01.md`.
